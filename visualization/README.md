Ce dossier peut contenir un cache local des SVG Dalton.

Pour télécharger les SVG officiels :

```bash
bash scripts/sync-dalton-data.sh
```

Sans cache local, l'application affiche les SVG depuis `raw.githubusercontent.com`.
